---
- hosts: "local_test"
  become: yes
  gather_facts: no
  vars:
    # - name: runadd-ssh-keys
    #   ansible.builtin.import_playbook: add-ssh-keys.yaml
    # - name: run create-user
    #   ansible.builtin.import_playbook: create-user.yaml
    # - name: run debug-packages
    #   ansible.builtin.import_playbook: debug-packages.yaml